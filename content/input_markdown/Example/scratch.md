# A file with no frontmatter

This is a complete, valid document. There is no frontmatter at all, and a file
at the top of a folder is fine.

Everything the generator could not establish — the date, the author, the
description — is recorded as *Unknown* and listed on the status page. Nothing
breaks.
